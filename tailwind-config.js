
tailwind.config = {
  theme: {
    extend: {
      colors: {
        brand: {
          bg: '#0f172a',      // Główne tło
          card: '#1e293b',    // Tło kart
          primary: '#f97316', // Pomarańczowy
        }
      }
    }
  }
}